{
  "name": "health-checkup-flow",
  "version": "1.0.0",
  "private": true,
  "description": "HealthFirst360 — combined health check-up queue management app",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build": "npm run build:frontend && npm run build:backend",
    "dev": "npm run build:frontend && npm run dev --prefix backend",
    "start": "npm run build && npm run start --prefix backend",
    "open": "npm run dev"
  }
}
